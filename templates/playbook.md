# Playbook — <Author>'s Methodology: <Domain>

> Built <YYYY-MM-DD> from <N> sources. This is the author's **procedural methodology** — the concrete steps, tactics, numbers, and checklists they teach — *not* how they think (that's `cognitive-model.md`). Every step cites evidence IDs from the evidence table (`clone.db`, via `clone.py`); audio/video citations carry a deep-link to the exact minute. Items marked `(inferred)` are not directly prescribed by the author.

## 0. Scope & promise
- **Domain:** <what this methodology is for, e.g. "growing an Instagram account from 0 to 100k">
- **Who it's for:** <the author's intended audience>
- **Core promise / outcome:** <what following it is supposed to achieve, in the author's words> [eIDs]
- **Named system (if any):** <the author's own name for their method/framework, verbatim> [eIDs]

## 1. The method at a glance
The end-to-end procedure as the author lays it out — the skeleton, before details.
1. <Phase/Step name> — <one line>
2. <Phase/Step name> — <one line>
3. …
> If the author gives an explicit ordering, preserve it. If not, reconstruct the most defensible sequence and mark it `(inferred sequence)`.

## 2. Step-by-step procedures
One block per step. This is the heart of the playbook — be concrete and actionable.
> **Provenance convention:** everything here is the author's unless tagged. Tag `(inferred)` for a step/connection the author implies but doesn't state, and `(inferred sequence)` for an order you reconstructed. Each step keeps a **short verbatim anchor quote** so the user can verify it against the author's own words. No step without a citation.

### Step <n> — <name>
- **What to do:** <the concrete action(s)>
- **Why (author's reasoning):** <short; link to cognitive-model if it maps to a belief> [eIDs]
- **How (tactics / exact moves):** <the specific tactics — bulleted, copy-pasteable where possible>
- **Numbers / thresholds:** <any concrete figures the author gives: post X times/week, first 3 seconds, aim for Y% — verbatim where stated> [eIDs]
- **Tools / resources named:** <apps, templates, services the author recommends> [eIDs]
- **Pitfalls / "don't do this":** <author's stated mistakes to avoid> [eIDs]
- **Source:** «<short verbatim quote>» — [<title>, MM:SS](<deeplink>) <+ more links if several sources>

## 3. Tactics & checklists
Standalone, do-this-now lists the user can act on without reading the whole method.
- **Checklist — <name>:** ☐ <item> ☐ <item> ☐ <item> [eIDs]
- **Tactic — <name>:** <one concrete tactic + when to use it> [eIDs]

## 4. Scripts / templates / formulas
Reusable artifacts the author hands out verbatim (hooks, message templates, formulas, content structures). Quote exactly; link the source.
- **<name>:** «<verbatim template/script>» — [<title>, MM:SS](<deeplink>)

## 5. Numbers, benchmarks & thresholds (quick reference)
A flat table of every concrete figure the author cites — fast lookup.

| Metric / lever | Author's number / rule | Context | Source |
|---|---|---|---|
| Posting frequency | <e.g. 1×/day> | <when/why> | [<title>, MM:SS](<deeplink>) |

## 6. Sequencing & decision points
- **Do X before Y when …** [eIDs]
- **If <situation> → do <branch A>; if <other> → <branch B>** (the author's "it depends" rules) [eIDs]

## 7. Common mistakes (author's stop-doing list)
- <mistake> — why it fails, per the author [eIDs] — [<title>, MM:SS](<deeplink>)

## Coverage note
- **Well-documented steps:** <which parts of the method are richly sourced>
- **Thin / inferred steps:** <which parts are sparse or reconstructed> ← lower confidence; say so when answering.
- **Not covered:** <parts of the domain the author never addresses in the corpus>
- **Fidelity self-check:** confirm every step traces to a citation, the sequence is the author's (or tagged `(inferred sequence)`), no generic best-practice was imported, and conditions/caveats were preserved. Note anything you're unsure faithfully represents the source.
